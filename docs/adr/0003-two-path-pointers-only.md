# 连接表单只放两个路径指针，配置语义单一来源在 config.json

状态：Superseded by ADR-0004（2026-09-23）。用户改为连接表单手填 servant，taf-mysql-mcp 及其 config.json 不再是插件运行依赖；"两路径指针"设计作废。最终表单字段见 CONTEXT.md：servant（text）+ allowWrite（checkbox）。
